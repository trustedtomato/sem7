googletrans==4.0.0rc1
httpx==0.13.3
mkl==2025.0.0
neurokit2==0.2.10
# We should use the same numpy version as the one used in the AI Lab container we're using
numpy==1.26.4
pandas==2.2.3
scikit_learn==1.5.2
scipy==1.14.1
tasks==2.8.0
# We should use the same torch version as the one used in the AI Lab container we're using
torch==2.5.0a0+b465a5843b.nv24.09
tqdm==4.66.6
transformers==4.46.2
wfdb==4.1.2
